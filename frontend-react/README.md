# Frontend (Vite + React + Tailwind)

Run locally:

```bash
cd frontend-react
npm install
npm run dev
```

Build for production (Vercel will run this):

```bash
npm run build
```

Environment:
- Use `.env` or Vercel project env var `VITE_BACKEND_URL` to point to backend API.
