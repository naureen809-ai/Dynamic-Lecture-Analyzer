{
  "name": "dynamic-lecture-analyzer-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix project/frontend-react && npm run build --prefix project/frontend-react"
  },
  "dependencies": {
    "groq-sdk": "^1.1.2"
  }
}
